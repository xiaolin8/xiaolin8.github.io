language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "xiaolin"
  - git config user.email "hu_xiao_lin@163.com"
  - git add .
  - git commit -m "Update blog"
  - git push --force --quiet "https://${GIT_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - blog-source
env:
 global:
   - GH_REF: github.com/xiaolin8/xiaolin8.github.io.git